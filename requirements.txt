numpy>=1.24
pandas>=2.0
pillow>=10.0
matplotlib>=3.7
PyYAML>=6.0
torch>=2.2
torchvision>=0.17

# Optional for diffusion/inference model loading:
accelerate>=0.28
diffusers>=0.27
safetensors>=0.4
transformers>=4.40

# Optional when GlobalLossAuxBundle(use_identity=True):
# Install separately with --no-deps so pip does not reinstall torch/torchvision.
#   pip install --no-deps facenet-pytorch

# Optional when GlobalLossAuxBundle(use_lpips=True):
lpips>=0.1.4
